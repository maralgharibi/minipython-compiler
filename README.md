🎯 پروژه طراحی کامپایلر: تحلیل نحوی و تولید AST
📖 فهرست مطالب
معرفی پروژه

ویژگی‌های زبان

نصب و اجرا

مثال‌ها

ساختار پروژه

معماری سیستم

نتایج تست

مستندات

🎯 معرفی پروژه
پیاده‌سازی تحلیلگر نحوی بازگشتی (Recursive Descent Parser) با تولید درخت نحو انتزاعی (AST) و الگوی Visitor برای زبان MiniPython.

هدف: کسب تجربه عملی در فازهای Front-End طراحی کامپایلر

✨ ویژگی‌های زبان (MiniPython)

✅ تعریف متغیر: var x = 10;

✅ عبارات ریاضی: با اولویت عملگرها + - * /

✅ عملگرهای مقایسه: > < == != <= >=

✅ عملگرهای منطقی: && ||

✅ کنترل جریان: if-else, while

✅ توابع: def func(x) { ... }

✅ فراخوانی تابع: print("Hello");

🚀 نصب و اجرا
پیش‌نیازها

Python 3.8 یا بالاتر

بدون نیاز به کتابخانه‌های خارجی

اجرا
bash
# حالت تعاملی
python3 main.py

# پردازش فایل
python3 main.py test.mpy

# اجرای تست‌ها
python3 test_cases.py
🎯 مثال‌ها
مثال ۱: برنامه ساده
python
# test1.mpy
var x = 10 + 5 * 2;
if (x > 15) {
    print("Result:", x);
}
خروجی:

text
✅ Parsing successful!
AST contains 2 statements

AST STRUCTURE:
Program:
  VarDeclaration: x
    Value:
      BinaryOp: +
        Left: IntegerLiteral: 10
        Right: BinaryOp: *
          Left: IntegerLiteral: 5
          Right: IntegerLiteral: 2
  IfStatement:
    Condition:
      BinaryOp: >
        Left: Identifier: x
        Right: IntegerLiteral: 15
    Then:
      Block:
        ExpressionStatement:
          CallExpression: print
            Arguments:
              StringLiteral: Result:
              Identifier: x
مثال ۲: توابع و حلقه
python
# test2.mpy
def factorial(n) {
    if (n == 0) {
        return 1;
    } else {
        return n * factorial(n - 1);
    }
}

var result = factorial(5);
print("5! =", result);
📁 ساختار پروژه
text
MiniPythonParser/
├── lexer.py          # تحلیلگر لغوی (توکن‌ساز)
├── parser.py         # تحلیلگر نحوی بازگشتی
├── ast.py            # کلاس‌های AST (15+ نود)
├── visitor.py        # پیاده‌سازی الگوی Visitor
├── main.py           # رابط خط فرمان
├── test_cases.py     # ۴ تست اصلی
├── grammar.txt       # گرامر زبان
└── README.md         # این فایل
🏗️ معماری سیستم
۱. Lexer (تحلیلگر لغوی)
تبدیل کد به توکن‌ها

تشخیص اعداد، رشته‌ها، شناسه‌ها

حذف کامنت و فاصله

۲. Parser (تحلیلگر نحوی)
Recursive Descent دستی

هر قاعده گرامری = یک متد

ساخت AST هنگام تحلیل

مدیریت خطا با بازیابی

۳. AST (درخت نحو انتزاعی)
python
class BinaryOp(Expression):
    def __init__(self, left, op, right):
        self.left = left
        self.op = op
        self.right = right
    def accept(self, visitor):
        return visitor.visit_binary_op(self)
۴. Visitor Pattern
جداسازی عملیات از ساختار

PrintVisitor برای نمایش درخت

قابل توسعه برای عملیات دیگر

🧪 نتایج تست
۴ تست اصلی:
✅ کد معتبر با تمام ویژگی‌ها - موفق

✅ ساختارهای تو در تو - موفق

✅ مدیریت خطا (کد نامعتبر) - موفق (تشخیص ۱۲ خطا)

✅ عبارات پیچیده - موفق

اجرای تست‌ها:
bash
$ python3 test_cases.py
============================================================
TEST 1: Valid code with all features ✅ PASSED
TEST 2: Nested structures ✅ PASSED  
TEST 3: Error handling ✅ PASSED
TEST 4: Complex expressions ✅ PASSED
============================================================
ALL TESTS PASSED ✓
📚 مستندات
گرامر کامل (در grammar.txt)
text
Program     → Statement*
Statement   → VarDecl | Assign | IfStmt | WhileStmt | FuncDecl | ReturnStmt | ExprStmt
VarDecl     → "var" ID "=" Expr ";"
IfStmt      → "if" "(" Expr ")" Block ("else" Block)?
FuncDecl    → "def" ID "(" ParamList? ")" Block
Expr        → LogicalOr
LogicalOr   → LogicalAnd ("||" LogicalAnd)*
...
اولویت عملگرها:
() پرانتز

+ - یکانی

* / ضرب و تقسیم

+ - جمع و تفریق

> < == != <= >= مقایسه

&& AND منطقی

|| OR منطقی

✅ خلاصه دستاوردها
✅ تحلیلگر بازگشتی دستی (بدون ابزارهای تولیدکننده)

✅ ساختار AST با سلسله مراتب صحیح

✅ الگوی Visitor برای عملیات روی درخت

✅ مدیریت خطا با پیام‌های گویا

✅ تمامی ویژگی‌های زبان پیاده‌سازی شد

✅ تست‌های جامع با پوشش کامل

👨‍🎓 اطلاعات پروژه
درس: طراحی کامپایلر

نوع: پروژه فردی

زبان پیاده‌سازی: Python

تاریخ: بهار ۱۴۰۳

